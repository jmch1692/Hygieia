name: GitGuardian scan

on: [push, pull_request]

jobs:
  scanning:
    name: GitGuardian scan
    runs-on: ubuntu-latest
    steps:
    - name: Trufflehog Check
      uses: UnicornGlobal/trufflehog-actions-scan@v1.0.0
      with:
        # Branch to run the check on
        branch: jmch1692-patch-1
